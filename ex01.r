nom <- "Adam"
age <- 18
est_etudiant <- TRUE

# ça afficher le type de chaque variable
print(class(nom))
print(class(age))
print(class(est_etudiant))

# pour créer une liste et l'afficher
profil <- list(nom = nom, age = age, est_etudiant = est_etudiant)
print(profil)
